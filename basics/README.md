# Basics

This folder contains **basic shell scripts** for learning the fundamentals of Bash.  

### Files in this folder
- `0-current_working_directory` – shows the current working directory  
- *(Add other scripts here as you create them)*

### Requirements
- Scripts must be run on **Ubuntu 20.04 LTS**  
- Each script should be **exactly 2 lines long**  
- All scripts must **end with a newline**  
- The first line of every script must be:
  ```bash
  #!/bin/bash
